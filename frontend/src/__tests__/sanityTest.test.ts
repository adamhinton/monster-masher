import { describe, it, expect } from "vitest";

describe("Sanity Test", () => {
	it("Reality is real, gravity still works. If this fails you have bigger problems - go outside, hug your family and pray for a swift and merciful end as reality rips itself apart.", () => {
		expect(1 + 1).toBe(2);
	});
});
